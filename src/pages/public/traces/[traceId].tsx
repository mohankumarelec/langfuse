// This url is deprecated, we keep this redirect page for backward compatibility

import TraceRedirectPage, {
  getServerSideProps,
} from "@/pages/trace/[traceId]";

export { getServerSideProps };

export default TraceRedirectPage;
